package main

import (
	"encoding/json"
	"fmt"
)

type Person struct {
	Name string `json:"name"`
	Age  int    `json:"age`
}

func main() {

	customer := Person{Name: "Martin", Age: 54}

	jsonData, err := json.Marshal(customer)

	if err != nil {
		fmt.Println("Json encode err: " , err)
	}

	fmt.Println(string(jsonData))


	
}